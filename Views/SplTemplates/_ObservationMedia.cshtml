@using MedRecPro.Models
@using MedRecPro.Helpers
@using RazorLight.Razor
@model ObservationMediaDto

<component>
    <observationMedia ID="@Model.MediaID">
        <value mediaType="@Model.MediaType" xsi:type="@Model.XsiType">
            <reference value="@Model.FileName" />
        </value>
    </observationMedia>
</component>